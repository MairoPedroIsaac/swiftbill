const { PrismaClient } = require('@prisma/client'); const prisma = new PrismaClient(); async function main() { const invoices = await prisma.invoice.findMany({ include: { lineItems: true }, orderBy: { createdAt: 'desc' } }); const badInvoices = invoices.filter(i => i.lineItems.length === 0); console.log('Found ' + badInvoices.length + ' bad invoices'); for (const inv of badInvoices) { await prisma.invoice.delete({ where: { id: inv.id } }); console.log('Deleted ' + inv.id); } } main().catch(console.error).finally(() => prisma.$disconnect());
